package TopBar;

import java.awt.*;

public class TopBarColor extends TopBar {

    /**
     * Constructor qui prend une couleur en paramètre
     * @param color coleur
     */
    public TopBarColor(Color color) {
        super();
        setBackground(color);
    }
}
